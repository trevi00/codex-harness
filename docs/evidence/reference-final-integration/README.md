# Final reference integration (implementation evidence)

Task `3fdbf869-26ef-4851-ae78-bfee57f5bf31`, starting at
`445fbc8859e896b90e974ed69447ce58e3446251`. The changes integrate the exact final
dependency candidate `91bd1ed363d92e0c286d7e807f87b7e72b597e31` and operator fix
`81d0274227d7c106fbad04fa4f64027b870d4995` in the assigned workspace. No merge,
push, deployment, production record update, or collection was performed.

`dependency-result.json` preserves the PostgreSQL result inspected before
selection, including the earlier failed attempts and final execution reference.
Its final candidate tree was verified as
`0aec074236fbe4c555b8d27a852f180bc53ecb71`. The row's stale top-level capacity error
is preserved alongside its explicit attempt outcomes; it is not a new error or
an independently recurrent failure. The dependency result is implementation
evidence, not an independent review.

`integration-source-verification.json` binds unchanged retention, occurrence V1,
tool-wait source/tests and all 25 prior copy entries. The decoder and reference
tests exactly match the operator revision, including six cross-record controls,
the 1,364 delimiter enumeration, and stronger incomplete-token expectations.

## New production blocker

The failed production dry run remains immutable at
`sha256:669cf5dc2c2489f73d6f5d7109850a14266c4ce0f1ad3f90658c3be5b55f0f65`.
The new parent is
`sha256:0a709c9ce8c3cd550e12f8a47c151bd884132fb6ec0c3ccb24c7861cccbb577b`,
selector `/independent_diff/diff`. Five exact UTF-8 intervals copy
`docs/evidence/malformed-prefix-rework/reproduction.json` from candidate
`b66e2017eb39643fd1128c1cf3b5173ea661991e`, tree
`3977a8bea048fa78ab017a78b677fa01d5a5b098`, base
`445fbc8859e896b90e974ed69447ce58e3446251`.

The proof authenticates commit/tree/blob bytes, absence at that base, the entire
added-file diff section, selector hash, and every token interval. Original
execution `sha256:6b145c4bc3834cc1af4c84543ae0ae922345d47eda894d4d4f6db76d8c31a52e`
event 821 contains the exact completed fixture-construction program; event 894
prints the exact reproduction blob. All three decoder source hashes are verified
against their Git bytes, including the recorded working-tree source against the
final historical candidate. The program is compared as data, never executed by
the provenance loader. Original results, including the rejected decoder's empty
results, remain unchanged. No artifact preimage was invented.

The resource now derives 30 copy entries over eight parents and retains the
separate 22 V1 entries. Both new original artifacts remain mandatory dependencies.
The scope is these five copied diff intervals; no archive-wide completeness or
classification of every token in the origin execution is claimed. Unproven
references in any origin remain blockers. Negative controls check altered
candidate/base/tree, changed diff bytes, changed source commands, missing output,
failed commands, and an equal-valued explicit artifact handle outside the diff.

## Validation and compatibility migration proposal

Command outcomes and content hashes are recorded in `validation.json` and the
adjacent logs. The six controls fail against the exact unchanged `11d79cc` decoder
and pass in the candidate focused suite. These expected control failures are
retained as failures, not counted as successful historical executions.

The unchanged incumbent reference tests from `445fbc8`, with their exact Git
fixture, produce **3 failed, 108 passed** against the candidate source:

- `test_clipped_diagnostic_json_excludes_only_complete_typed_image_tokens`
- `test_partial_docker_word_needs_complete_local_identity_and_output_context`
- `test_clipped_identity_tokens_bind_only_to_complete_declarations_in_same_artifact`

Each expects an unresolved occurrence to disappear based on typing elsewhere.
INV-RESOURCE-001 now conservatively retains it. The first scratch run omitted a
file-relative fixture and produced a fourth setup failure; its separate log is
preserved. The corrected run removes only that setup failure. Neither run is
the host release evaluator or full incumbent integration qualification.

Proposed migration, requiring explicit independent review: adopt the operator's
three stronger expectations as an intentional compatibility change in the
incumbent baseline through the authorized baseline-migration process, preserving
these failed qualification results. Independently review the retention rationale
and six omission controls first; then run normal qualification against the newly
authorized baseline and exact candidate. This implementation does not rewrite
incumbent tests, alter evaluator policy, skip tests, or waive the current gate.
Until that migration is approved and applied, the incumbent gate remains failed.

Keep collection paused pending a complete current host production traversal and
writer convergence. The host owns the long scan; none was started or awaited
here. Independent lead/conductor reviews, actual fresh exact-candidate CLI
canaries, full host incumbent/candidate service qualification, and the reviewed
compatibility migration remain required under INV-RELEASE-001. Failed command
inspection is inspection-blocked, never approval. Local tests and skipped
integration tests do not satisfy these gates.
